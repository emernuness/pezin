{
  "name": "@pack-do-pezin/worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --env development",
    "dev:remote": "wrangler dev --env development --remote",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env development",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.99.0"
  }
}
